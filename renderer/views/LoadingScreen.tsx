import { FC } from "react";

const LoadingScreen: FC<{
  fadeState: "opacity-control-in" | "opacity-control-out";
}> = () => {
  return <div id="LoadingScreenBox">asdada</div>;
};

export default LoadingScreen;
